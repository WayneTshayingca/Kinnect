/**
 * Re-exported from @kinnect/hooks — import directly from there in new code.
 * This shim keeps all existing `@/hooks/useShoppingPresence` imports working.
 */
export { useShoppingPresence } from '@kinnect/hooks'
