/**
 * Re-exported from @kinnect/core — import directly from there in new code.
 * This shim keeps all existing `@/lib/formatters` imports working.
 */
export { toLocaleDateStr, getTodayStr, formatEventDate, formatEventTime, timeAgo } from '@kinnect/core'